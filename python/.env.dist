# --- Provider selection (defaults = Google everywhere) ---
# RAG: vertex (Vertex AI) | memory (in-memory, not shared across processes) | pgvector (PostgreSQL, shared)
RAG_PROVIDER=vertex
# LLM: gemini (Google) | openai (OpenAI) | groq (Groq, free tier)
LLM_PROVIDER=gemini
# Storage: gcs (Google Cloud Storage) | local (filesystem) | minio (S3-compatible, e.g. local MinIO)
STORAGE_PROVIDER=gcs

# Required when LLM_PROVIDER=gemini: Gemini API key (https://aistudio.google.com/apikey)
GEMINI_API_KEY=your_gemini_api_key_here

# Required when RAG_PROVIDER=vertex or STORAGE_PROVIDER=gcs. See docs/GOOGLE_SETUP.md.
GCP_PROJECT_ID=gen-lang-client-0048900827
GCS_BUCKET_NAME=geminimesh
GCS_DOCUMENTS_PREFIX=agents
VERTEX_REGION=europe-west2
VERTEX_RAG_INDEX_ENDPOINT_ID=your-index-endpoint-id
VERTEX_RAG_INDEX_ID=your-index-id
# Deployed index ID on the endpoint (e.g. from findNeighbors: deployedIndexId in the request)
VERTEX_RAG_DEPLOYED_INDEX_ID=your-deployed-index-id

# Database: same URL for dev (local Postgres) and prod (Cloud SQL). Example prod: postgresql://user:pass@35.197.251.64:5432/geminimesh
# Required when RAG_PROVIDER=pgvector. Run: alembic upgrade head (includes pgvector extension + rag_embeddings table).
DATABASE_URL=

# Optional when RAG_PROVIDER=pgvector: table name (default rag_embeddings), embedding dim (default 768, match BAAI/bge-base-en-v1.5)
# RAG_PGVECTOR_TABLE=rag_embeddings
# RAG_EMBEDDING_DIM=768

# Optional: GeminiMesh API (for /update_agent_prompt_geminimesh)
GEMINIMESH_API_URL=https://api.geminimesh.com/api
GEMINIMESH_API_TOKEN=
# Timeout in seconds for POST to GeminiMesh (default 90; increase if backend is slow)
GEMINIMESH_REQUEST_TIMEOUT=90

# Required when LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-...

# Required when LLM_PROVIDER=groq (free at https://console.groq.com/keys)
# GROQ_API_KEY=gsk_...

# Optional when STORAGE_PROVIDER=local: base path for files (default: data/storage)
# LOCAL_STORAGE_PATH=data/storage

# Required when STORAGE_PROVIDER=minio (e.g. docker run -p 9000:9000 -p 9001:9001 minio/minio server /data --console-address ":9001")
# MINIO_ENDPOINT=localhost:9000
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin
# MINIO_BUCKET=documents
# MINIO_SECURE=false
# MINIO_PREFIX=agents

# Optional: Server
HOST=127.0.0.1
PORT=8000
