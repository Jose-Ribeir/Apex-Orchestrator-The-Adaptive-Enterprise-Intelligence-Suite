# Apex Orchestrator – main .env (repo root)
# Used by: docker compose up (from repo root)
# Web: http://192.168.1.222:3000  |  API: http://192.168.1.222:4000  |  MinIO: http://192.168.1.222:9001

# -----------------------------------------------------------------------------
# LLM (set at least one; Groq or Gemini)
# -----------------------------------------------------------------------------
LLM_PROVIDER=groq
GROQ_API_KEY=
# Or use Gemini (supports multiple keys for 429 fallback):
# LLM_PROVIDER=gemini
# GEMINI_API_KEY=your_key_here
# GEMINI_API_KEYS=key1,key2,key3

# -----------------------------------------------------------------------------
# Postgres (defaults work with docker-compose)
# -----------------------------------------------------------------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=ai_router
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# Redis
# -----------------------------------------------------------------------------
REDIS_PORT=6379

# -----------------------------------------------------------------------------
# MinIO (defaults work with docker-compose)
# -----------------------------------------------------------------------------
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=documents
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# -----------------------------------------------------------------------------
# App ports (host)
# -----------------------------------------------------------------------------
API_PORT=4000
WEB_PORT=3000

# -----------------------------------------------------------------------------
# Frontend URLs (as seen by the browser; use your LAN IP for network access)
# -----------------------------------------------------------------------------
VITE_API_URL=http://192.168.1.222:4000
VITE_APP_URL=http://192.168.1.222:3000

# CORS: comma-separated origins allowed for API (must include VITE_APP_URL when using credentials)
CORS_ORIGINS=http://192.168.1.222:3000,http://localhost:3000
